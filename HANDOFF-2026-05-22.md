# Paid — handoff for revenue-readiness

**Date:** 2026-05-22 · **Operator:** Claude (Polsia mode)

This is the package for getting Paid from "running, no customers" → "Marketplace-listed and pilot-ready." Everything Claude could do alone is done. Everything below is for Tommy because it requires his face, voice, identity, or click.

---

## What's done (in this session)

### Logo + brand
- `public/marketplace/icon-{16,32,48,64,96,128,180,192,256,512,1024}.png` — full size ladder
- `app/icon.png`, `app/apple-icon.png`, `app/favicon.ico` — Next.js auto-favicon
- `app/opengraph-image.png` — social cards (1200x630)
- `public/marketplace/social-cover.png`, `marketplace-banner.png`, `lockup-horizontal.png` — store assets
- `public/logo-paid.svg`, `mark-paid.svg` — vector for components
- `app/layout.tsx` — `metadata.openGraph` + `metadata.twitter` wired

### Code fixes — P0 (blocked pilots)
- **Onboarding → checkout 400**: `app/api/stripe/create-checkout/route.ts` now resolves `email` from Supabase session if absent in body. No more "Invalid payload" on the trial start.
- **No initial QB sync after connect**: `app/api/auth/quickbooks/route.ts` now uses Next.js `after()` to fire `syncInvoicesForUser` post-redirect. Merchant lands on Dashboard with real data, not 24h-old emptiness.
- **QB N+1 customer fetch**: `lib/quickbooks/sync.ts` + `client.ts` — batch `fetchCustomerEmailsByIds` with chunked parallel IN-queries. 50-invoice firm: ~10s → ~1s.
- **Cron sequential timeout**: `app/api/cron/daily/route.ts` — users now processed in parallel chunks of 5. No more "first 20 users get processed, rest starve."

### Code fixes — P1 (data safety + perf)
- **Stripe webhook idempotency**: `app/api/stripe/webhook/route.ts` checks `processed_stripe_events` table, short-circuits on duplicate delivery before any side effects.
- **Composite indexes migration**: `supabase/migrations/20260522000000_perf_and_idempotency.sql` — adds `(user_id, days_overdue desc)`, `(user_id, quickbooks_invoice_id)`, plus the idempotency table.
- **Settings null-safe**: cron's settings lookup uses `.maybeSingle()` not `.single()` — no more crash on users missing their settings row.
- **Chunked invoice upsert**: 500-row chunks instead of single statement.

### OAuth submission — partial
- Branding page (App name, URLs, support email, contact) **already filled** — confirmed via Chrome MCP.
- All other tabs (Audience, Data Access, Verification Center) need your hand. See §3 below.

---

## What Tommy must do

### 1. Review the code diff, then commit + push (15 min)

```
cd C:\Users\yanto\.cursor\projects\empty-window\paid
git diff --stat                          # see what's changed
git diff app/api/stripe/create-checkout/route.ts     # spot-check
git diff lib/quickbooks/sync.ts          # spot-check
git add .
git commit -m "Logo + favicon, P0/P1 fixes (checkout email, initial QB sync, batched customer fetch, parallel cron, webhook idempotency)"
git push
```

Vercel auto-deploys on push to main. Watch the deploy at https://vercel.com/dashboard (1-2 min).

### 2. Run the migration on Supabase prod (3 min)

Open https://supabase.com/dashboard → Paid project → SQL Editor → New query. Paste the contents of `supabase/migrations/20260522000000_perf_and_idempotency.sql`. Click **Run**. Expect "Success. No rows returned."

### 3. Finish the OAuth submission in Google Cloud Console (30 min)

Open https://console.cloud.google.com/auth/branding?project=paid-493720.

**a. Branding tab (already 90% done — just need the logo)**
- Click **Browse files** under "Logo file to upload"
- Pick `C:\Users\yanto\.cursor\projects\empty-window\paid\public\marketplace\icon-128.png`
- Click **Save**

**b. Audience tab**
- User type: **External**
- Test users: add your demo Gmail + 2-3 friendly first customers
- Publishing status: leave **In Testing** until verification clears

**c. Data Access tab (the big one — 7 scopes to add)**

Click **Add or remove scopes**. In the right-side panel, search and tick each of these one at a time, then click **Update**:

```
https://www.googleapis.com/auth/gmail.addons.execute
https://www.googleapis.com/auth/gmail.addons.current.message.metadata
https://www.googleapis.com/auth/gmail.addons.current.message.readonly
https://www.googleapis.com/auth/gmail.addons.current.action.compose
https://www.googleapis.com/auth/script.external_request
https://www.googleapis.com/auth/script.locale
https://www.googleapis.com/auth/userinfo.email
```

For each sensitive scope (the 4 `gmail.addons.*` and the `readonly`), there's a justification field. Paste from `marketplace/OAUTH-VERIFICATION-SUBMISSION.md` — each scope has a "Why we need it" line ready.

**d. Verification Center tab**
- Paste the **Limited Use justification** from §2 of `marketplace/OAUTH-VERIFICATION-SUBMISSION.md` (the 6-line paragraph starting "Paid's use and transfer to any other app...")
- Paste the **App description** from §4 of the same file

### 4. Record the demo video (45 min — only thing only you can do)

Follow `marketplace/DEMO-VIDEO-SCRIPT.md` line by line. Critical:
- Demo account, not your real one
- One overdue QB invoice with a known client email
- One inbox thread with a fake "can I pay next week?" reply
- Record at 1080p, ~3 min total
- Upload to YouTube **Unlisted**
- Title: `Paid — OAuth verification demo (paid-app.com)`
- Paste the URL into the Verification Center tab

### 5. Submit (1 click)

Verification Center → **Submit for verification**. Email reviewers same-day on follow-ups. Realistic: 3-6 weeks to approval.

### 6. Set up support@paid-app.com (10 min, can happen anytime before submit)

- Google Workspace → Users → Add user → email `support@paid-app.com`
- Forward to your inbox so reviewer emails reach you
- Update the User Support Email in the Branding tab from `tommy.yan.16@gmail.com` to `support@paid-app.com`

Without this, Google reviewers might flag the consent screen for using a personal Gmail.

### 7. Verify paid-app.com in Search Console (5 min)

- https://search.google.com/search-console
- Add property `paid-app.com`
- Verify via DNS TXT record (Vercel domains lets you add it)
- Required for "Authorized domains" in the consent screen

---

## What stays open after all 7 steps

- Marketplace SDK listing — can be done in parallel; instructions in `marketplace/CASA-AND-CHECKLIST.md` Phase 3. Submit when OAuth verification clears.
- Apps Script add-on v1.1.0 deploy — parked per earlier call. Resurface when first paying user requests the new identity-exchange.

---

## Round 1 customer discovery (also still alive)

Files in `gtm/` (built earlier this session):
- `gtm/01-targets.md` — 10 boutique A/E firms with named decision-makers
- `gtm/02-emails.md` — 2 cold email variants A/B
- `gtm/03-linkedin.md` — day-4 fallback sequence
- `gtm/04-discovery-script.md` — 30-min interview structure
- `gtm/05-tracker.xlsx` — pre-loaded tracker
- `gtm/README.md` — hour-1 path

**Recommendation:** send those 10 emails on Tuesday morning, regardless of where OAuth submission is at. Discovery calls don't need an approved Marketplace listing — they need a thing to demo. Paid already demos.

---

## Reality check on time-to-revenue

- **OAuth + Marketplace approval timeline**: 5-8 weeks from full submission
- **Pilot revenue without OAuth**: possible TODAY — the Workspace Add-On still works for users you invite directly (Testing mode allows up to 100 test users). Public install is what's blocked.
- **First-customer path that doesn't depend on OAuth**: invite 5-10 design partners as Testing-mode test users → run discovery → close → install as test user → use the product → bill via Stripe.

The OAuth approval is for *public Marketplace listing*. For your first 10 customers, you can sidestep it entirely by adding them as test users on the consent screen.

That's the unblock: **start selling Tuesday with what you already have.**
